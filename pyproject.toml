[project]
name = "sfutils-pat"
version = "0.1.0"
description = "Snowflake PAT (Programmatic Access Token) manager - sfutils CLI package"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.0",
    "keyring>=25.0",
    "python-dotenv>=1.2.1",
    "requests>=2.28",
    "snowflake-cli>=3.16.0",
    "snowflake-connector-python>=3.13.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.15.11",
]

[project.scripts]
sfutils-pat = "sfutils_pat:cli"
check-setup = "sfutils_pat.check_setup:check"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sfutils_pat"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes (unused imports, undefined names)
    "W",    # pycodestyle warnings
    "I",    # isort (import sorting)
    "UP",   # pyupgrade (modern Python syntax)
    "B",    # flake8-bugbear (common bugs & design issues)
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
    "N",    # pep8-naming conventions
    "RUF",  # ruff-specific rules
    "PL",   # pylint
]
ignore = [
    "PLR0911",  # Too many return statements — unavoidable in multi-branch dispatch functions
    "PLR0912",  # Too many branches — unavoidable in complex CLI handlers
    "PLR0913",  # Too many arguments — unavoidable in Click command functions
    "PLR0915",  # Too many statements — unavoidable in complex CLI handlers
    "PLR2004",  # Magic value comparisons — intentional for masking constants
    "PLW0602",  # global without assignment — intentional module-level state pattern
    "PLW0603",  # global statement — intentional module-level state pattern
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-v --tb=short"
